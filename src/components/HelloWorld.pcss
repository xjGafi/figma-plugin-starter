.hello-world--container {
  .hello-world--option-box {
    padding: 8px;
    border-bottom: 1px solid var(--silver);
  }

  .hello-world--button-box {
    display: flex;
    justify-content: space-around;
    padding: 8px;
  }
}
